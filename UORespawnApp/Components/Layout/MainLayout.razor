@inherits LayoutComponentBase
@inject DebugService DebugService
@inject ViewService ViewService
@inject BackgroundDataLoader BackgroundDataLoader
@using UORespawnApp.Scripts.Services
@using UORespawnApp.Scripts.Utilities
@using UORespawnApp.Components.Controls
@implements IDisposable

<LoadingIndicator />
<ToastNotification />

@if (_showPendingEditsModal)
{
    <PendingEditsModal OnClose="ClosePendingEditsModal" />
}

@if (_showServerUpdateModal && _serverUpdateInfo != null)
{
    <ServerUpdateModal UpdateInfo="_serverUpdateInfo" OnClose="CloseServerUpdateModal" />
}

@{
    // Hide debug panel on home page (splash screen)
    var showDebugPanel = DebugService.IsEnabled && ViewService.CurrentView != "home";
}

<div class="page @(showDebugPanel ? (isDebugCollapsed ? "debug-mode-collapsed" : "debug-mode") : "")">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <article class="content px-4">
            @Body
        </article>
    </main>

    @if (showDebugPanel)
    {
        <DebugPanel OnCollapseChanged="OnDebugCollapseChanged" />
    }
</div>

@code {
    private bool isDebugCollapsed = false;
    private bool _showPendingEditsModal = false;
    private bool _showServerUpdateModal = false;
    private ServerUpdateService.ServerUpdateInfo? _serverUpdateInfo = null;

    protected override void OnInitialized()
    {
        DebugService.OnDebugModeChanged += OnDebugModeChanged;
        ViewService.OnViewChanged += OnViewChanged;
        BackgroundDataLoader.PendingCommandsDetected += OnPendingCommandsDetected;
        BackgroundDataLoader.ServerUpdateAvailable += OnServerUpdateAvailable;
    }

    private void OnDebugModeChanged(bool enabled)
    {
        InvokeAsync(StateHasChanged);
    }

    private void OnViewChanged()
    {
        InvokeAsync(StateHasChanged);
    }

    private void OnDebugCollapseChanged(bool collapsed)
    {
        isDebugCollapsed = collapsed;
        StateHasChanged();
    }

    private void OnPendingCommandsDetected(object? sender, EventArgs e)
    {
        _showPendingEditsModal = true;
        InvokeAsync(StateHasChanged);
    }

    private void ClosePendingEditsModal()
    {
        _showPendingEditsModal = false;
        StateHasChanged();
    }

    private void OnServerUpdateAvailable(object? sender, ServerUpdateService.ServerUpdateInfo info)
    {
        _serverUpdateInfo = info;
        _showServerUpdateModal = true;
        InvokeAsync(StateHasChanged);
    }

    private void CloseServerUpdateModal()
    {
        _showServerUpdateModal = false;
        _serverUpdateInfo = null;
        StateHasChanged();
    }

    public void Dispose()
    {
        DebugService.OnDebugModeChanged -= OnDebugModeChanged;
        ViewService.OnViewChanged -= OnViewChanged;
        BackgroundDataLoader.PendingCommandsDetected -= OnPendingCommandsDetected;
        BackgroundDataLoader.ServerUpdateAvailable -= OnServerUpdateAvailable;
    }
}
